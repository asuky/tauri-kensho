import React from 'react';
import { CardHeader as _CardHeader } from '@mui/material';
/*
import {
    as _
} from '@mui/material';

export default function _(props) {
    return (<_ {...props}>{props.children}</_>);
}
*/

export default function CardHeader(props) {
    return (<_CardHeader {...props}>{props.children}</_CardHeader>);
}