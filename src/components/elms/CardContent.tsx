import React from 'react';
import { CardContent as _CardContent } from '@mui/material';
/*
import {
    as _
} from '@mui/material';

export default function _(props) {
    return (<_ {...props}>{props.children}</_>);
}
*/

export default function CardContent(props) {
    return (<_CardContent {...props}>{props.children}</_CardContent>);
}